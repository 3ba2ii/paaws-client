query AdoptionPosts($cursor: String, $limit: Int) {
  adoptionPosts(cursor: $cursor, limit: $limit) {
    posts {
      id
      pet {
        id
        name
        type
        gender
        size
        birthDate
        images {
          photoId
          petId
          photo {
            url
          }
        }
      }
      address {
        distance
        country
        state
      }
      createdAt
      updatedAt
      user {
        full_name
      }
    }
    hasMore
    errors {
      field
      message
      code
    }
  }
}
