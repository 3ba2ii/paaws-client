fragment MissingPostFragment on MissingPost {
  id
  title
  descriptionSnippet(length: $length)
  voteStatus
  user {
    id
    displayName
    avatar {
      url
    }
  }
  commentsCount
  tags
  address {
    distance
  }
  points
  thumbnail {
    url
  }
  createdAt
  updatedAt
}
